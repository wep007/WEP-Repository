name: Fitst workflow
on:
  push:
    branches:
        - "main"

jobs:
    lab-job:
        run-ons: ubuntu-latest
        steps:
            - name: Start job
              run: echo 'Hello DevOps'
            - name: Second Step
              run: echo 'This is second step'
            - name: Third Step
              run: echo 'This is third step'
    second-job:
        run-ons: ubuntu-latest
        steps:
            - name: Start job
              run: echo 'second job'
